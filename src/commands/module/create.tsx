import { resourceDescription, resourceCommand, resourceOptions } from "../../command-helpers.js";
export const description = resourceDescription("module", "create");
export const options = resourceOptions("module", "create");
export default resourceCommand("module", "create");
