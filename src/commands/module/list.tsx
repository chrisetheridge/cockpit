import { resourceDescription, resourceCommand, resourceOptions } from "../../command-helpers.js";
export const description = resourceDescription("module", "list");
export const options = resourceOptions("module", "list");
export default resourceCommand("module", "list");
