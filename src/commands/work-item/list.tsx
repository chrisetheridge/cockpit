import { resourceDescription, resourceCommand, resourceOptions } from "../../command-helpers.js";
export const description = resourceDescription("work-item", "list");
export const options = resourceOptions("work-item", "list");
export default resourceCommand("work-item", "list");
