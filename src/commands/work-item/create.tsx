import { resourceDescription, resourceCommand, resourceOptions } from "../../command-helpers.js";
export const description = resourceDescription("work-item", "create");
export const options = resourceOptions("work-item", "create");
export default resourceCommand("work-item", "create");
