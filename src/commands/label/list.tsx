import { resourceDescription, resourceCommand, resourceOptions } from "../../command-helpers.js";
export const description = resourceDescription("label", "list");
export const options = resourceOptions("label", "list");
export default resourceCommand("label", "list");
