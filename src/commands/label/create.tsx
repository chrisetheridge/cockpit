import { resourceDescription, resourceCommand, resourceOptions } from "../../command-helpers.js";
export const description = resourceDescription("label", "create");
export const options = resourceOptions("label", "create");
export default resourceCommand("label", "create");
