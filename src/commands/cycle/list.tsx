import { resourceDescription, resourceCommand, resourceOptions } from "../../command-helpers.js";
export const description = resourceDescription("cycle", "list");
export const options = resourceOptions("cycle", "list");
export default resourceCommand("cycle", "list");
