import { resourceDescription, referenceArgs, resourceCommand, resourceOptions } from "../../command-helpers.js";
export const description = resourceDescription("cycle", "remove-item");
export const options = resourceOptions("cycle", "remove-item");
export const args = referenceArgs;
export default resourceCommand("cycle", "remove-item");
