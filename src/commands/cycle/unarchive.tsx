import { resourceDescription, referenceArgs, resourceCommand, resourceOptions } from "../../command-helpers.js";
export const description = resourceDescription("cycle", "unarchive");
export const options = resourceOptions("cycle", "unarchive");
export const args = referenceArgs;
export default resourceCommand("cycle", "unarchive");
