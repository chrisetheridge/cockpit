import { resourceDescription, resourceCommand, resourceOptions } from "../../command-helpers.js";
export const description = resourceDescription("cycle", "create");
export const options = resourceOptions("cycle", "create");
export default resourceCommand("cycle", "create");
