import { resourceDescription, resourceCommand, resourceOptions } from "../../command-helpers.js";
export const description = resourceDescription("state", "create");
export const options = resourceOptions("state", "create");
export default resourceCommand("state", "create");
