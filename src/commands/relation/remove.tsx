import { resourceDescription, resourceCommand, resourceOptions } from "../../command-helpers.js";
export const description = resourceDescription("relation", "remove");
export const options = resourceOptions("relation", "remove");
export default resourceCommand("relation", "remove");
