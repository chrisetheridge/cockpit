import { resourceDescription, resourceCommand, resourceOptions } from "../../command-helpers.js";
export const description = resourceDescription("relation", "list");
export const options = resourceOptions("relation", "list");
export default resourceCommand("relation", "list");
