import { resourceDescription, resourceCommand, resourceOptions } from "../../command-helpers.js";
export const description = resourceDescription("relation", "add");
export const options = resourceOptions("relation", "add");
export default resourceCommand("relation", "add");
