import { resourceDescription, resourceCommand, resourceOptions } from "../../command-helpers.js";
export const description = resourceDescription("project", "create");
export const options = resourceOptions("project", "create");
export default resourceCommand("project", "create");
