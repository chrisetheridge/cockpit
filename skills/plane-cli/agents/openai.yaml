interface:
  display_name: "Plane CLI"
  short_description: "Operate Plane safely through its agent-first CLI"
  default_prompt: "Use $plane-cli to inspect or update Plane through deterministic, machine-readable commands."
